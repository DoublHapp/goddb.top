# goddb.top 部署环境变量模板
# 复制为 deploy/.env（不入库），并替换为真实值：
#   cp .env.example .env
#
# PostgreSQL 数据库密码（必填，后端与 postgres 服务共用）
POSTGRES_PASSWORD=change-me-strong-password
# 管理接口 Bearer Token（必填，用于删除评论等管理操作，随机长串）
GODDB_ADMIN_TOKEN=change-me-random-token
